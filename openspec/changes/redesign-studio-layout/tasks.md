<!--
Orthogonal intents (max 3):
1. [2026-09-19 Delivery] 骨架先行（五区视口布局可跑）→ 逐区拆装（检查器/胶片带/状态条）→ 对比覆盖层收尾（唯一新交互件）。
2. [2026-09-19 Safety] 每步全量绿门 + 移动端回归硬承诺；八态逐态测试；主区无纵向滚动结构性断言。
3. [2026-09-19 Process] 与 add-asset-library 并行时只依赖选图器签名；空态双 CTA 若其未就绪，先以占位接口对接。
-->

## 1. 骨架重写（五区固定视口）

- [ ] 1.1 StudioView 重排：上下文条/画布 flex/检查器 320px/胶片带/状态条五区布局（min-h-0/min-w-0 链）；主区零纵向滚动（结构性断言入测试）；画布参数调整全程可见走查
- [ ] 1.2 StudioContextBar：来源信息+更换入口 + 预览三模式 pills/透明度迁入 + 取景控制（适应/±/百分比）；vitest+走查：预览模式即时生效

## 2. 逐区拆装

- [ ] 2.1 Inspector（自 BlockPanel 拆装）：选中块详情置顶（同视口零滚动）+ 物理/色板/分块/块列表折叠组；无选中=全局态；组件本体复用
- [ ] 2.2 StrategyFilmStrip（自 CompareGrid 拆）：五策略 chips+钻数+合规点+选中高亮（点击=导出策略唯一写入点）+ hover 预览浮卡（180px）
- [ ] 2.3 StudioStatusBar（自 ExportBar 演化）：答案位大数字+策略回显+校验徽标+BOM 前3色…▾+导出组+送精修；违规时红徽标+[边界松弛][斥力修复]浮出+清单▾；导出禁用语义与 spacing 门不变（vitest 迁移）
- [ ] 2.4 previewRender 共享模块抽取（FilmStrip/CompareOverlay 复用 gemPaint 管线）；基准：与现 CompareGrid 渲染同源同速

## 3. 对比模式（覆盖层）

- [ ] 3.1 CompareOverlay：全屏覆盖（Esc/点外部退出，focus trap）+ 五栏大图（约 20vw）+ 点卡=设为导出策略并退出
- [ ] 3.2 A/B 滑动对比器（选两策略左右拖分屏；P0 可降级「两栏并排+点击切换」最小形态，滑动器 P0.5）；vitest+走查

## 4. 移动端同构

- [ ] 4.1 画布 flex 填充（60vh 定值废除）+ 胶片带横滑 chips（停驻≠选中）+ 状态条导出菜单（SVG/BOM/PNG/送精修）
- [ ] 4.2 对比模式=全屏 Sheet（大图横滑 scroll-snap + 底部[设为导出策略]）；移动端现行为回归（抽屉/半屏选中块）测试兜底

## 5. 收尾

- [ ] 5.1 八态逐态测试（空态/解码/分块/重算/选中/违规/对比/移动选中）+ 既有 mount/interactions 选择器迁移；全量绿门
- [ ] 5.2 浏览器走查：312px+375px 工作台；「调整→画布全程可见」断言；记分卡复测（Journey 5→9 目标；发布会截图测试=能）
- [ ] 5.3 与 add-asset-library 收尾互查（空态双 CTA/上下文条更换/送精修入口迁移 C-4 三交汇点）
