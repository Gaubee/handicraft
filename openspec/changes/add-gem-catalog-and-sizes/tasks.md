<!--
Orthogonal intents (max 4):
1. [2026-09-19 Contract] [R2 终审 W0-GATE-ONLY 授权范围，与 design §0.1/§1.7 一致]：
     0.x（W0 v2 contract gate，零业务实现——任意 add-project-files 2.x serializer 开工前的硬前置，R2 P0-4）
       -> 1.x（engine gate，可整体并行于 2.3 之外的实现面；1.5 的 CVT 优化独占——性能敏感，不与其他引擎改动并行）
       -> 2.1 / 2.3 / 2.4（内置规格 seed 目录 / 四格式迁移完整实现 / 常量收编切换——三者可并行，均只消费冻结类型）
       -> 2.2（.gemshape 八面 vertical slice；硬前置 = 0.4 + add-project-files 1.1 AssetProject 实现可用，未落地则顺延）
       -> 3.x（收尾 gate：全量绿门 + 评审）
   约束：并行实现代理上限 2；全量 pnpm test/check/build 绿门串行执行；W0 验收 = R1 P0-1 修订后的 contract receipt（0.7，
   R2 P0-1 前两项验收分句 + P0-2 全文），行为测试分句在 1.7/3.2 最终合流门逐字引用收口。
2. [2026-09-19 Data] canonical 类型唯一化 = specKey 唯一持久身份；specId 持久化别名与二/三参 helper 并存
   在 W0 grep 清零；四格式 v2 迁移 fixture 全 vitest 证明（byte-round-trip/向前拒读/脏输入）。
3. [2026-09-19 Engine] 混合径 pairwise/exportGate/BOM 新键/SVG 逐钻 = engine gate；布局五策略不动（单 spec 输入）；
   CPU deterministic oracle 不动摇；CVT 优化输出逐位不变（不 bump）；GPU 不进 P0。
4. [2026-09-19 Process] 本 change 不做：图层化/专家工作台 UI/蓝图双任务/replay-handoff-export 层化消费（design §0.2）；
   两项工作默认待 Owner 批准可推翻（design §0.4）；每步绿门 pnpm test + svelte-check + build。
-->

## 0. W0 v2 contract gate（第一实现单元；类型 + typed error + 迁移入口 + fixture，零业务实现）

- [x] 0.1 canonical 类型唯一化（单独提交）：`BaseSpec` / `GemSpecSnapshot{specKey,ordinal,shapeId,sizeLabel,diameterMm,widthMm?,heightMm?,assetId?,rotationDeg?}` / `PhysicalCanvas{widthMm,heightMm,anchorSource}` / `GemSpec`（内存条目）唯一类型模块；`GridSpec` v2 派生化（+`gapMm`、移除 `ss` 语义、`gridFromSpec` 新入口、`gridFromSs` 降位圆钻特例）；rotationDeg 非身份显式注释；vitest：类型编译 + 旧 GridSpec 消费者（buildBom `g.ss`、ProjectSummary.ss、stores/edit.svelte.ts:514 摘要构造——R1 裁断 4 补登）迁移清单登记
- [x] 0.2 唯一几何 helper 冻结：`requiredCenterDistancePx(a: GemSpecSnapshot, b: GemSpecSnapshot, grid: GridSpec): number` + `maxCellPx(specs, grid)`（单位恒 px，mm→px 换算只在 helper 内；R2 §三-1 定案签名）；vitest：helper 契约测试（等径=pitch 等价、大小径包络、maxCellPx ≥ 任意对所需距离）+ 全库唯一调用面断言
- [x] 0.3 四格式 v2 版本表 + 迁移入口：`PROJECTFILE_FORMAT_VERSIONS`/`LABFILE_FORMAT_VERSIONS` bump 至 2（projectFile.ts:51-56 / labFile.ts:37-41）；注册 v1→v2 迁移入口（纯函数，gemproj 单 rest 层 / gemdoc 补 round+查表直径 / gemtpl 补 drillParams/blueprint 正交键（缺席=两开关关——Owner 2026-09-20 裁决二，键形消费规范源=add-lab-drill-params-and-blueprint）+gemSpecIds / gemgen 拆 requestMode（endpoint）+正交快照+可选 blueprint/gemSpecs/physicalCanvas 键位）；LayerRecord 类型定义冻结（宿主实现归 studio-layers）；vitest：每格式 v1 fixture 迁移演练 + v2 save→load→save 字节等价 + 向前拒读 + 脏输入 typed error
- [x] 0.4 `.gemshape` schema gate 六条（先于 renderer；出处 gemspec R1 P0-6）：parser 冻结——解码后实际宽高不符拒收 / MIME·字节·像素上限 / alpha bounds 非空（主径取 alpha bounds）/ fit 纵横比超容差拒绝 / reference 校准 refSpecId 可解析或内嵌 refSpecSnapshot / missing 资产 typed 禁静默降级导出；**texture 必备 + vectorPath 可选加速（R3 P0-1 修复冻结：vector-only 非法——typed error 拒收；并存导出优先矢量）**；`ProjectKind`+`PROJECT_MIME` 第五值；vitest：六条各一坏输入用例 + **fixture 矩阵（texture-only / both 两类合法输入 round-trip + vector-only 坏输入 typed error 拒收断言）** + 校准烘焙函数（direct/reference 两模式）
- [x] 0.5 物理锚契约 + 常量单源出口：`pixelsPerMmFromCanvas(canvasWidthPx, canvas|undefined)` 签名冻结（锚定实际降采样 image.width；缺失/非法回退 2.5 且 `anchorSource:'default'` 显式；dimsMismatch 以实测为准）；engine 单一出口 `PIXELS_PER_MM` 常量定义（三处副本切换归 2.4）；vitest：px/mm 不变量（2048→1024 降采样、非正方形、缺失回退）
- [x] 0.6 目录资产 schema 冻结 + 迁移 bootstrap 表（Owner 2026-09-20 裁决一）：`.gemshape` schema 增 `vectorPath?`（归一化 SVG path，单位框 0..1——可选渲染加速字段：贴图必备、vector-only 非法 typed error 拒收、并存导出优先 vectorPath——R3 P0-1）/`specKey?`（canonical 身份键——三处必填时点条件矩阵见 design §1.4：seed 必填如 round-ss10，v1 文件输入与 custom ingest 可缺席、按 custom-<assetId> 派生——R3 非阻塞 1）；seed 数据形状（形 id/短码/中文名/贴图必备+可选 vectorPath/物理宽高）+ P0 五形（round/square/drop/heart/marquise）+ 身份不可变纪律（specKey 创建后不可变、seed 只读、改物理=另存副本；**`.gemshape` 内容不可变——不参与 blobKey 换绑，任何内容变更=另存新资产（新 assetId/新 specKey）——R3 P0-2**）；engine 迁移 bootstrap 表（SS_TABLE 直径查表 + specKey 生成规则 round-ss10/square-3.5——非目录真源）；vitest：specKey 确定性 + path 归一化（单位框）+ seed 幂等 create-only（`ast-shape-${specKey}` 节点存在即跳过含软删）+ **texture-only/both 合法 fixture 与 vector-only 拒收断言（与 0.4 fixture 矩阵同源）**
- [x] 0.7 **W0 contract receipt（R1 P0-1 修订：只验收契约面）**——① TypeScript 编译 + canonical 名称 grep（`specId` 身份字段/持久化别名清零；`refSpecId` 为独立校准引用字段、单独定义不计入禁令）；② helper 签名冻结（`requiredCenterDistancePx(a,b,grid)` 三参唯一 + `maxCellPx`，无二参/别名并存）；③ 四格式 v1 fixture 迁移演练 + v2 byte-round-trip + 向前拒读 + 脏输入 typed error；④ `.gemshape` 六条 gate 坏输入用例 + texture 必备/vector-only 拒收（0.4 fixture 矩阵）；⑤ PhysicalCanvas px/mm 不变量（**仅 schema/纯函数层，运行时接线不在 W0 验收面**）；⑥ serializer 首写证明：2.x 首个写入路径仅在 W0 receipt 通过后出现且不存在 v1 新写路径（写路径 grep）。R2 §四 P0-1 原文中「跨层 mixed-size/旋转/边界测试全部通过」依赖 engine/replay 实现——**移至 1.7 与最终合流门（3.2）收口，W0 不验收行为测试**；P0-2 验收原文（byte-round-trip/向前拒读/损坏·超限·悬空 ref 拒绝/missing 不得静默导出）与 gemspec R1 放行条件 1（「不接受只扩字段、不扩 `GridSpec`/handoff 的切片」）整条属本 receipt

  > **W0 contract receipt（2026-09-20 六项全过；实施 rhinestone-studio d37b2a4→7790b69）**
  > - **① TS 编译 + canonical grep**：svelte-check `0 errors / 0 warnings`；`specId` 身份字段/持久化别名（exact-key：`specId:`/`specId =`/引号键）src 全域 grep **零命中**；`refSpecId` 为 .gemshape 校准独立引用字段（persistence/gemshapeFile.ts 单独定义，豁免不计）；断言落测 `spec.contract.test.ts` 10/10（含 GridSpec.ss 旧消费者迁移清单现场核对：export.ts `g.ss` / projectTypes `ProjectSummary.ss` / edit.svelte.ts:514——engine gate 1.3/1.4 迁移后删）。
  > - **② helper 签名冻结**：`requiredCenterDistancePx(a, b, grid)` 三参 + `maxCellPx(specs, grid)` 唯一定义点 `engine/geometry.ts`（grep 证据：定义仅 geometry.ts，index.ts 公共再输出，其余为注释引用；无二参/别名/同义函数）；契约测试 `specGeometry.test.ts` 8/8（等径=pitch 等价、大小径包络、rotationDeg 圆包络不变性、gap 单一来源、maxCellPx ≥ 任意对、空清单 0、50 组随机性质保持）。
  > - **③ 四格式 v2**：版本表 {gemproj:2, gemdoc:2}/{gemtpl:2, gemgen:2}；v1 fixture 迁移演练（gemproj 单 rest 层 round-ssXX 派生+overrides 搬运、gemdoc 全 gems 补 round+SS_TABLE 直径含 m- 手工钻+grid 补 gapMm、gemtpl 直通归一、gemgen mode→requestMode 只读映射）+ v2 save→load→save **字节等价**（四格式 + v2 全键形态：physicalCanvas/blueprint+role 标记/gemSpecs/正交快照）+ 向前拒读（v3）+ 脏输入 typed error（迁移入口即拒：physics.ss/grid.pitchMm；LayerRecord 分区不变量：零/多 rest、跨层重复块拒收）；`formatV2.test.ts` 19/19；既有 projectFile/labFile 测试 v1 断言已更新至 v2 口径（round-trip/键序/版本号全绿）。
  > - **④ .gemshape 六条 gate**：解码实测宽高拒收 / MIME 白名单+字节(4MiB)+像素(2M)上限 / alpha bounds 非空 / fit 容差(2%)拒绝 / reference 悬空防线（refSpecId 可解析或内嵌 refSpecSnapshot 二者其一）/ missing 四态 typed 禁静默导出（`assertGemshapeRefs`——GemshapeRefError「不回退圆钻轮廓」）；**fixture 矩阵：texture-only / both 两类合法 round-trip + vector-only typed error 拒收**；校准烘焙 direct/reference 两模式；`ProjectKind`+`PROJECT_MIME` 第五值（gemshape+json）；`gemshape.test.ts` 19/19。
  > - **⑤ PhysicalCanvas px/mm 不变量（仅 schema/纯函数层）**：`pixelsPerMmFromCanvas(canvasWidthPx, canvas|undefined)` 签名冻结——2048→1024 降采样锚定**实测交接宽**（1024÷210，不盲用文件记录宽）、非正方形（只由宽派生）、缺失/非法回退恒 `PIXELS_PER_MM`（2.5）且 anchorSource 两值显式（schema 拒第三值/缺席）；engine `PIXELS_PER_MM` 单一出口定义于 engine/spec.ts（grep 证据：engine 域唯一 `export const PIXELS_PER_MM = 2.5`；三处旧副本 studio.svelte.ts:56 / gemprojReplay.ts:41 / quickLayout.ts:65 保持未切——收编切换归 2.4，W0 不动消费面）；`physicalAnchor.test.ts` 5/5。
  > - **⑥ serializer 首写证明**：`serializeGemproj` **非测试调用方为零**（grep 证据——add-project-files 2.x 写路径未开工，首个 gemproj 写入路径只能在 W0 之后出现）；四格式现存写路径（gemdoc=edit store 保存链 / gemtpl=seed+模板编辑 / gemgen=归档链）均经 `*_FORMAT_VERSIONS` 版本常量**恒写 v2**，src 内硬编码 `formatVersion:1` 写入 grep 零命中（.gemshape `formatVersion:1` 为第五格式 v1 起步既定，非四格式 v1 回写）。
  > - **附（0.6）**：目录资产 schema（GemshapeSeedSpec：形 id/短码/中文名/贴图必备+可选 vectorPath/物理宽高）+ P0 五形元数据 + 身份不可变纪律注释（specKey 不可变、.gemshape 内容不可变不参与 blobKey 换绑）+ 迁移 bootstrap 表 `ROUND_SS_BOOTSTRAP`（SS_TABLE 直径 × round-ssXX 派生，非目录真源）+ seed 幂等 create-only 计划器（`ast-shape-${specKey}` 存在即跳过含软删）；`specCatalog.test.ts` 10/10。
  > - **W0 六测试文件合计 71/71**；偏离登记：gemdoc v2 gems 的 shapeId/diameterMm 过渡期可选（1.4 Gem/EditGem 字段落地后转必填）；gemproj parse 产物携带 v1 兼容派生读面（physics/activeStrategy/overrides，供未迁移 gemprojReplay 消费——engine/replay gate 迁移后删除；非圆钻 specKey 的读面派生显式 typed error 拒绝，不静默猜值）；gemgen serialize 输入接受 deprecated `mode` 写键（序列化边界映射为 canonical requestMode，产物不落 mode——add-lab change 迁移 lab store 写面后移除）。R2 §四 P0-1 第三项验收分句（跨层 mixed-size/旋转/边界行为测试）按 R1 P0-1 修订移至 1.7/3.2 收口，**W0 不验收行为测试**。

## 1. engine gate（第二实现单元）

- [x] 1.1 pairwise 实现：`requiredCenterDistancePx`/`maxCellPx` 落地；`SpatialIndex` cell 从 pitch → maxCellPx（ops.ts:199 / spatialIndex.ts:15-16 消费面）；vitest：恰跨 cell 边界邻域检索不漏
- [x] 1.2 校验/冲突混合径化：`validate`（validate.ts:17-40）/`validateEditable`（edit.ts:53-76）/`resolveConflicts→resolveGreedy`（edit.ts:130-137 / conflict.ts:27-45）单一 pitch → 逐对 helper；布局五策略/relax 输入仍单 spec（产物入文档强制 pairwise gate）；`exportGate` 纯函数（SVG/BOM/PNG/送精修共同前置；保存 warning、导出硬阻断）；vitest：大小径混合/边界 gap/旋转异形圆包络不变性/20k 钻/违规清单确定性排序
- [x] 1.3 导出升级：`buildSvg` 逐钻规格渲染（round circle 快路径保留、异形 path 缩放、自定义 `<image>`；`gemRadiusPx(grid)`→`gemRadiusPx(gem, grid)`，grid.ts:46-48）；`buildBom` 聚合键 `colorId` → `specKey×colorId`，表头 `规格,形状,尺寸,色名,hex,数量`（export.ts:89-103 改造）；vitest：三渲染路径 golden + BOM 新键（同规格不同自定义资产 assetId 区分行 + 合计行）
- [x] 1.4 `Gem`/`EditGem` 字段落地：shapeId/diameterMm/rotationDeg?/assetId?（types.ts:68-91）+ zod 公共契约面同步 + `EditGemFields` 白名单扩展（rhinestone-studio/src/lib/stores/edit.svelte.ts:126，x/y/colorId → +shapeId/diameterMm/rotationDeg）；vitest：round-trip + v1 缺省补 round
- [x] 1.5 CPU oracle + CVT 性能 P0（独占切片，不与其他引擎改动并行）：同参快照确定性断言；CPU CVT 优化（repairSpacing 轮数/早停、像素累加降采样/增量 Delaunay、medianColor 计数化）目标 43s→<10s @1024² **输出逐位不变**（gpu-research.md §7 P0-1 基线 43.1s）；vitest：优化前后同参快照逐位相等 + 计时基准（阈值 <10s）。**[研发前置已完成 2026-09-20，结论见 .agents/documents/2026-09-19-gem-catalog/ 下 cvt-optimization-research.md 与 cvt-opt-reference.ts：推荐路线 a = 同序定容网格 + 扁平 pair 缓冲（替换 repairSpacing.pairs() 与 applyRepulsion.violatingPairs 的 Map 索引；cell 同 pitch、同扫描/插入/追加序 → Gauss-Seidel 顺序不变）——1024² 四类图 + relax 最坏 4.8-7.0s 全部达标、12+ 组合快照逐位相等、加速 2.9-4.3×、落点 ±120 行集中 cvt.ts/relax.ts；早停/像素表/inlineFind/hypot→sqrt/medianColor 均为负结果勿走；43.1s 基线疑含负载（本机安静 min 15-25s），落地后用原始合成图复测]**
- [x] 1.6 `ENGINE_VERSION` 1→2（validate/conflict/export 语义变更；version.ts:16）：gemproj 序列化自动携带新值 + 重放漂移横幅语义核对；护栏：单规格圆钻文档 v1/v2 引擎钻位逐位不变对比测试
- [x] 1.7 **engine gate 验收（出处标注）**——R2 §四 P0-3（engine 部分）：「engine gate 完成逐钻 spec、pairwise/exportGate……多层导出包含隐藏层且违规硬阻断；BOM 使用 `specKey×colorId`」（多层导出接线归 studio-layers，判据/门在本切片证明）；R2 §一 P0-2：「统一签名、单位和 `maxCellPx` 公式；实现全层 concat 的 pairwise、保存 warning/导出 hard block；补大小径、跨 cell、边界 gap、旋转、20k 测试」；**R2 §四 P0-1 行为分句（R1 P0-1 移入）：「跨层 mixed-size/旋转/边界测试全部通过」在本切片收口**（单层行为测试 + 判据证明；跨层 concat 组织与 replay 联合验收归 replay/handoff gate）；gpu-research §7 P0-1：43s→<10s 零输出变化

  > **engine gate receipt（2026-09-20 实施完成；rhinestone-studio 7f162d3→23bd262 六提交，逐切片 solo 绿门）**
  > - **逐切片交付与聚焦测试**：1.1 `7f162d3` cell 契约迁移（geometry PairwiseSpec/effectiveSpecOf + grid baseSpecDiameterMm 量化回推 + validate/validateEditable cell=maxCellPx + ops/edit 索引 cell 契约文档）——cellIndex 4/4（恰跨桶边界混合径对不漏 + 30 组随机混合径性质测试：暴力法零漏零重）；1.2 `d2e66b0` 判据单一 pitch×0.999 → 逐对 `requiredCenterDistancePx×0.999`（等径 = v1 逐位等价）+ resolveGreedy 签名 (gems,pitch,compare)→(gems,grid,compare)（cell=maxCellPx；检索/贪心序不变——hybrid-dropped 旧路径镜像等价测试实证）+ `exportGate.ts` 纯函数（spacing/mask/missing-asset 三面 + kind 秩→gemIds 字典序确定性排序）——mixedPairwise 11/11（大小径混合/边界 gap 相切含等号/旋转圆包络不变性/20k 钻 383ms 双运行逐位一致/违规清单确定性/exportGate 全 face 含多层 concat 硬阻断）；1.3 `2fddfcd` buildSvg 三渲染路径（round circle 快路径 v1 字节形态保留/异形 vectorPath 单位框×diameterMm×ppm 缩放含旋转/custom `<image>` 并存优先矢量/custom 未解析=虚线占位+data-missing 不静默降级）+ buildBom 聚合键 canonical specKey×colorId（表头 规格,形状,尺寸,色名,hex,数量；gemSpecIdentityOf 投影：custom→custom-<assetId>、round 精确命中 ROUND_SS_BOOTSTRAP→round-ssXX、其余 builtinSpecKey mm 档）+ gemRadiusPx 逐钻签名（@deprecated 单 grid 过渡重载保留给未迁移 components）——exportSpec 10/10 三路径 golden + BOM 新键全 face；1.4 `065890b` Gem/EditGem +shapeId/diameterMm（必填）/rotationDeg?/assetId? + GemSchema/EditGemSchema（宿主 spec.ts——SHAPE_IDS 单源，规避 types↔spec 值导入环）+ makeGem(ctx,…) 源头规格戳 + EditGemFields 白名单扩展 + **GridSpec.ss 过渡读面删除**（三处消费者清零：buildBom/ProjectSummary/edit store 摘要；parseGrid 容忍并剥离旧文件 ss 键）+ gemdoc v2 钻位字段转必填（W0 过渡窗口偏离项收口）——gemFields 6/6 + spec.contract ss 清零断言改写 + 29 处测试 fixture 迁移（svelte-check 本 gate 域 0 err/0 warn）；1.6 `c0fa5cc` ENGINE_VERSION 1→2（bump 纪律注释）——versionGuard 6/6（v1 判据参考实现全等直证 + hex-pitch 黄金现场抽查 + serialize 恒携带 2 + v1 引擎旧档可 parse）；1.5 `23bd262` CVT 路线 a 独占落地（cvt repairSpacing + relax violatingPairs → 同序定容网格 + 扁平 pair 缓冲；负结果路线均不采）——cvtPerf + engineGolden。
  > - **R2 §四 P0-3（engine 部分）**：「engine gate 完成逐钻 spec、pairwise/exportGate」✓（1.4 物化字段 + 1.2 判据/门）；「多层导出包含隐藏层且违规硬阻断」判据证明 ✓（mixedPairwise 多层 concat 测试 + 1.7 engineAcceptance 三层 concat×mixed-size×旋转×跨 cell 组合：违规逐对命中/确定性/硬阻断、修复后全层放行——隐藏层枚举与层序组织归 studio-layers，接线不在本 gate）；「BOM 使用 `specKey×colorId`」✓（exportSpec BOM 新键 + assetId 区分行 + 合计行）。
  > - **R2 §一 P0-2（闭合表）**：「统一签名、单位和 `maxCellPx` 公式」✓（W0 冻结 helper 全消费——validate/validateEditable/resolveGreedy/exportGate 唯一调用面，几何单位恒 px，mm→px 只在 helper 内）；「实现全层 concat 的 pairwise、保存 warning/导出 hard block」✓（exportGate violations 供保存路径降级呈现 warning、导出路径 ok=false 硬阻断——语义分句在 mixedPairwise/engineAcceptance 证明）；「补大小径、跨 cell、边界 gap、旋转、20k 测试」✓（mixedPairwise + cellIndex 全 face）。
  > - **R2 §四 P0-1 行为分句（R1 P0-1 移入本切片收口）**：「跨层 mixed-size/旋转/边界测试全部通过」✓（engineAcceptance 2/2：三层 concat 组合行为 + 旋转不变性跨层再证；跨层 concat 组织与 replay 联合验收归 replay/handoff gate——design §2.6 出处标注）。
  > - **gpu-research §7 P0-1（43s→<10s 零输出变化）**：✓ 逐位不变 = engineGolden 黄金守卫 10/10（**v1 引擎动工前采集**，含 400² boston 高细节 + relax 最坏组合——1.1 起每切片复跑，1.5 优化后仍逐位相等）；计时 = 512² 默认档确定性+宽松上限（~1.6s/次）；**1024² 原始合成图复测（CVT_PERF_1024=1 显式档）：high cvt 6.28s / cvt+relax 6.50s（17384/9315 钻）——落在研发预估带 4.8-7.0s 内，<10s 达标；ENGINE_VERSION 不参与 bump（输出逐位不变）**。
  > - **GridSpec.ss 清零证据（grep）**：`rg "ss\??:\s*SSKey|GRIDSPEC_SS_MIGRATION_CHECKLIST" src/lib` → GridSpec 类型面 ss 键与迁移清单常量零命中（残余命中均为 SS 函数参数//bootstrap 行字段/`GemprojFile.physics.ss` v1 派生读面——后者 replay gate 迁移后删除，W0 receipt 既定）；`grid.ss|g.ss` 读面零命中（唯一残余 = projectFile v1→v2 迁移入口读原始 v1 grid 数据——design §1.1 明文允许「旧 v1 fixture 仅在迁移入口读取 ss」）；ProjectSummary.ss 零命中；gridFromSs 产物无 ss（spec.contract 断言 `'ss' in grid === false`）。
  > - **偏离登记**：① gemRadiusPx 保留 @deprecated 单 grid 过渡重载（(pitch−gap)/2×ppm = v1 SS_TABLE 查表值逐位同值）——EditCanvas.svelte/gemPaint.ts 两处 components 消费面按本 gate 文件域禁改约束未迁移，studio gate/rename-and-expert-workbench 组件迁移后删除；② exportGate missing-asset 面在 resolveShapeAsset 未接线时跳过（运行时接线归 2.2 vertical slice/studio gate——判据面已测）；③ buildBom 的 round specKey 投影消费 ROUND_SS_BOOTSTRAP 直径精确匹配（engine 无 IO 无目录——非「显示码/浮点格式化反推」，canonical 键空间唯一来源；baseSpecDiameterMm 量化回推保证构造链逐位命中查表值）；④ 旧 v2 过渡文件 grid 携带 ss 键经 parseGrid 剥离后不再写回（信息无损——pitch+gap 可派生；round-trip 字节等价以新写面为基准）；⑤ 收尾 `pnpm check` 存在 4 err/1 warn，全部位于并行工作流未提交的 components/Edit/{alignDistribute,properties,EditPropertiesPanel} 新文件（rename-and-expert-workbench 在途产物，非本 gate 文件域——engine/edit-lib/persistence/stores-edit 域 0 err 0 warn，全量 engine+edit 套件 307 passed+1 skipped（1024 显式计时档））。


## 2. 目录与资产化实现（第三波）

- [ ] 2.1 内置规格 seed 落地：sys-shapes 下按 `ast-shape-${specKey}` 幂等 seed（P0 五形 round/square/drop/heart/marquise × 圆形 SS 档 / 异形 mm 档，含贴图（必备）+ 可选 vectorPath 数据 + 物理宽高——裁决一与 R3 P0-1 口径，目录真源为素材库资产、engine 仅留迁移 bootstrap，见 design §1.6/§3.1）；**SS24 补档 = 新增一条 seed 条目**（SS_KEYS 现缺，types.ts:12-25；同参同出不 bump，随 ENGINE_VERSION 纪律注释登记，R2 非阻塞建议）；vitest：seed 幂等（含软删跳过）+ 目录枚举确定性 + SS24≈5.3mm 入 seed + 既有档位数值零变化
- [ ] 2.2 `.gemshape` 八面 vertical slice（硬前置 0.4 + add-project-files 1.1；gemspec R1 议题 10「一次性冻结」）：projectTypes 第五值 / AssetNode 接线（**内容不可变——同 gemgen 纪律，不参与 blobKey 换绑（R3 P0-2）；编辑=另存自定义副本，节点改名等元数据可改**）/ App 导入路由（ingest→素材库定位）/ sys-shapes 系统目录 seed（插「模板」与「生成结果」之间）/ RightSheet 编辑器（去使用·去编辑两 canonical handler；**编辑器=查看+另存副本入口，无就地改内容（design §3.2 面 5）**；seed 资产只读，编辑=另存为自定义——design §1.6 身份纪律）/ serialize-parse + 迁移 / 引用 pin-GC 矩阵（弱引用 missing 四态 + 编辑期 pin 校准参考 + 硬清走既有 GC + 删除后 missing typed + 导出阻断）/ 校准向导数据面；vitest：seed 幂等 / ingest+另存副本（内容不可变——断言无 blobKey 换绑入口/换绑路径为零） / missing 四态 / 导出阻断（gate 6 端到端）
- [ ] 2.3 四格式 v1→v2 迁移完整实现：迁移链填充（复用注册骨架 projectFile.ts:113-128 / labFile.ts:106-121）+ 全量 round-trip 家族（含 gemproj overrides 块键搬运 / gemdoc m- 前缀手工钻 / gemgen provenance.mode 旧档只读映射）；vitest：五格式（含 .gemshape v1 起步）byte-round-trip 矩阵 + 迁移链断环拒绝
- [ ] 2.4 常量收编切换：三处 `PIXELS_PER_MM` 副本（studio.svelte.ts:56 / gemprojReplay.ts:41 / quickLayout.ts:65）→ import engine 单一出口；quickLayout 显式 default 标注；vitest：常量单源断言（三消费方同值）+ 行为零变化（同参快照）

## 3. 收尾 gate

- [ ] 3.1 全量绿门：`pnpm test`/`pnpm check`/`pnpm build` 全绿；既有基线债处置——~~`gemgenArchive.test.ts` 终态持久化竞态~~ **已于 2026-09-19 修复（c930a01：whenIdle 排干归档链，archiveDepth 计数器；solo 8/8 + 兄弟 33 绿）**；R2 §四 P0-5 验收「66/66 files、864/864 tests」的全量 receipt 仍欠，留低负载窗口补跑后在此登记
- [ ] 3.2 Codex 评审 → 修订 → 归档候选；**最终合流门（R1 P0-1）：逐字引用 R2 §四 P0-1 全文收口**——「验收：TypeScript 编译、全库 grep 只有 canonical 名称、跨层 mixed-size/旋转/边界测试全部通过」（前两项验收分句=W0 receipt、第三项验收分句=engine gate 1.7 + replay 联合），与 R2 §四 P0-2 验收原文、gemspec R1 放行条件 1 一并逐条复核 + 消费面矩阵（studio-layers §E.6）中本 change 责任行的测试符号核对
