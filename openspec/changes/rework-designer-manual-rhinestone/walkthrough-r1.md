# 贴钻工作台 R5.2 Vision 走查记录（vision-walk3）

- 日期：2026-09-21 · 走查人：vision 子代理（ego-browser 0.5.0.32 / chromium 152）
- 目标：http://localhost:5210 （vite dev server，主会话持有，未重启/未杀）
- TaskSpace：spaceId=23「贴钻 R5.2 vision 走查门」，已 `finish({keep:[]})`
- 样本图：/Users/kzf/Pictures/贴钻/01-src.jpg（794×794）
- 无 BYOK key；为展开 RunBar/WYSIWYG 预览面，配置了**假连接** `https://example.invalid/v1 / sk-dummy-for-ui-walk / gpt-dummy`（未成功发出任何真实请求；一次误触开始生成向假端点发车后已「取消全部」，任务画廊留下 16 张失败/取消记录）
- 每张截图均程序化验图（非黑非纯白像素比 ≥2% 为有效；黑底特写类两帧 1.87%/2.32% 因拍摄对象本身近全黑，已结合内容判读并在下文标注）

## A 设计师工作台

| # | 项 | 动作 | 预期 | 实察 | 截图 |
|---|---|---|---|---|---|
| A1 | 空态入口 | 切换 tab | 三入口 | **4 个入口**（选图新建/空白新建/打开/上传图片新建），清单说三入口——待复核口径 | 02 |
| A1 | 选图新建 | 选图→素材库 Dialog→上传 01-src→确定 | 参考底图+0 钻+状态栏 | 底图渲染完整；状态栏 0 钻/R10/间距 3.2mm/笔刷 2.8mm·流量 100%/画幅 317.6×317.6mm·2.5px/mm（缺省锚）；图层面板含参考底层 | 03 |
| A2 | 落钻 | R10 选形器选「金」、画笔落 6+ 颗 | — | 落钻/计数（0→6）读数正确；R10 触发钮出现金色圆点 | 04,05 |
| A2 | 纹理 sprite | 454%/650%/1334% 特写 | 银白材质图着色、非纯色圆 | **内部平坦的金色圆片+扇贝边，无切面/高光纹理**；亮度扫描 std=14.9（主要来自描边）；resource 面板 0 张图片资源（无材质图集加载）→ 疑似程序化纯色绘制，**不符 R2 预期** | 10,11,16 |
| A2 | 柔投影 | 米色/黑底观察 | 带柔投影 | **浅底上无可见投影晕**（56/16 号帧钻周无渐变暗晕） | 16 |
| A2 | hover 增强 | 选择模式精确悬停钻心 | 投影增强 | 区域像素差 8.4% ≈ 光标本体像素量级，**无可辨识增强**——待复核（低于感知阈值或未实现） | 18 vs 16 |
| A2 | 选中发光 | V 点选 | 蓝色发光描边 | **蓝辉光描边清晰** ✓；属性面板同步（颜色/形状/尺寸/朝向/图层） | 13 |
| A3 | 笔刷直径 | `[`×4 / `]`×4/×8 | 光标盘变大+读数 | 读数 2.8→4.8→6.8mm、`[` 回 2.8mm ✓（108% 下圆盘过小，650% 下可见） | 19,20 |
| A3 | 单列画线 | 2.8mm 竖拖 200px | 单列 | +16 颗，细单列链（蜂窝路径）✓ | 23 |
| A3 | 多列铺开 | 4.8mm/6.8mm 横拖 300px | 铺多列 | 4.8mm +36；6.8mm **+112（≈3 行密带）**，视觉成带 ✓ | 21,24 |
| A3 | 笔刷 popover | 点状态栏笔刷读数 | 直径+流量滑杆 | 「笔刷设定」：直径 number + 流量滑杆（range，蓝轨）+ 跟随规格直径 + 帮助文案 ✓。**注意：popover 打开时吞键盘快捷键（⌘Z 失效），点画布外部也不关，仅触发钮可 toggle——P2** | 25,26 |
| A3 | 流量 30% | 设 30% 再画 | 稀疏对比 | +48 vs 100% 时 +112，视觉明显稀疏 ✓ | 26 vs 24 |
| A3 | 橡皮 | E 拖过色带 | 红圈 footprint+批量擦 | 拖拽中**红圈光标** ✓，224→197（-27）批量擦 ✓ | 27 |
| A4 | 点选/加选 | 点/Shift 点 | 1→2 | ✓ | — |
| A4 | 框选/Alt 减选 | 拖框/Alt 拖框 | 6→4 | ✓ | 28,29,30 |
| A4 | 拖移/Alt 复制 | 拖动/Alt 拖动 | 数量不变/+$n | 拖移 12→12 且选区保持 ✓；Alt 拖 12→16(+4) ✓ | 31,32 |
| A5 | ⌘T 进入 | CDP 派发 Meta+T（浏览器保留键，普通按键注入到不了页面） | 包围盒+柄 | 进入 ✓：包围盒+4 角柄+4 外柄；圆钻选集外柄正确灰显（opacity .5/pointerEvents none） | 38,40 |
| A5 | 角柄缩放 | 拖 nw 外拉 | 钻位不动只变大；实时读数 | 拖拽中出现 **288% 读数气泡** ✓；但**画布内钻尺寸不实时预览、松手 pending 丢失、Enter 后实测直径仍 2.8mm（量测 2.88/2.49/2.84/2.88mm）→ 变换不生效（P1）** | 40,41,43,44 |
| A5 | Enter/Esc | — | 单撤销组/取消 | Enter 退出变换盒 ✓；Esc 退出 ✓（Esc 同时清选区）；因缩放未生效，单撤销组无法独立验证 | — |
| A5 | ⌥[ ⌥] | CDP Alt+]/Alt+[ | 旋转 15° | 朝向字段 0→30→60 步进 ✓（属性面板）；视觉旋转因形状渲染问题不可见（见下） | 49 |
| A5 | 非圆钻视觉 | 属性改形状=马眼 | 旋转视觉实时 | **画布把马眼仍渲染成正圆**（黑底干净背景复核）→ 形状/朝向不反映到画布渲染（P1/P2 待复核） | 49,50 |
| A5 | 规格选择器选形 | 点「马眼」后落钻 | 新钻为该形 | 新钻仍是圆钻（选择疑似未写入笔刷规格）——待复核 P2 | — |
| A6 | 智能排布退役 | 顶栏+右键空态树 | 均无入口 | 顶栏无 ✓；右键空态菜单=粘贴/全选当前层/画幅设置…/适配画幅/100%，无智能排布 ✓（负验收 PASS） | 45 |
| A7 | 图层操作 | 新建/重命名/隐藏/锁/合并/移入 | — | 新建 ✓、重命名（行内「图层重命名」输入）✓、移入选中钻 ✓（所属图层→测试层）、隐藏 ✓、锁定后钻不可选中 ✓、向下合并（测试层并入图层1）✓。**疑点：一次新建的图层2 在重命名流程后消失（待复核）** | — |
| A7 | 导出隐藏层确认门 | 隐藏层后导出 SVG→取消 | 弹确认、取消零产物 | 「导出将不含 1 个隐藏层（1 颗钻）——隐藏层内容不进产物。仍要导出？」✓；取消后无下载事件、无弹窗残留 ✓ | — |
| A8 | 画幅 popover | 点状态栏画幅读数 | — | 「画幅锚定 宽317.6·高317.6mm 2.5px/mm 锚来源：缺省锚 改声明」✓（该 popover 点外部/Esc 不关，P2） | 51 |
| A8 | 保存/打开/最近 | ⌘S（CDP）→命名保存→关闭文档→最近列表→重开 | — | 保存框 ✓（默认名「设计 · 未命名」可改）、保存后「未保存」徽标消失 ✓、最近列表显示 R52走查项目.gemdoc ✓、重开后 22 钻/图层结构保留 ✓ | 51 |
| A9 | ? 速查 | Shift+/ | 新快捷键行 | 「键位速查」含 ⌘T（角柄等比/外柄旋转/Enter/Esc）、[/]、⌥[⌥]、Alt+框选、方向键微移、⌘A、⌘⇧N、⌘E 等行 ✓ | 34 |

## B 提示词实验室

| # | 项 | 实察 | 截图 |
|---|---|---|---|
| B10 | 终稿即所见 | RunBar（含「最终请求提示词」）仅 configured 时渲染（无连接时整块缺失——本轮用假连接展开）。模板1 三开关全关后：预览条目与编辑器正文**逐字相等**（归一空白后 exact match，76 字符）；全部 9 个预览条目中「你是专家/【模板风格补充】/【案例参照图提示词】/图序/输出行/贴钻指导规则」零残留 ✓ | — |
| B11 | 案例开关+片段 Dialog | 案例开关开→预览自动物化图序声明：「我上传了一张图片：1.【图一 [image #1]：案例参照图】…请参考【图一 [image #1]…」✓（[image #N] 字面 ✓）。片段 Dialog：textarea 空 +「当前：自动生成（预览）·留空保存=恢复自动生成」、可编辑 ✓——**Dialog 内不直接展示当前自动文案本体**（P2 透明度） | 52 |
| B12 | 六点面 | ①TextQuote 入口（效果开关旁 icon button）开 Dialog ✓ ②[image #N] 字面 ✓ ③水钻开+尺寸空：模板块出现中文必填红字「画幅物理尺寸＊（必填）…请填写宽高（发起生成时将拦截未填写的模板）」✓，**但点开始生成后任务仍入队并向端点发请求（1.8s 失败）——拦截语义与文案不符，待复核**（同时观察到模板钻块「画幅物理尺寸」被自动填 210×148，与我清空的全局图宽/图高是两个字段）④高级参数：可视化/JSON 预览/JSON 编辑三 Tab 在 DOM、未知字段（新增自定义字段键名/值）✓、图宽/图高 input ✓；尺寸快选 chips Dialog 未能通过自动化展开（懒加载零尺寸，待复核）⑤蓝图缩略空态：蓝图开→「蓝图参考图 0/2·从素材库选…（至多 2 张，随蓝图请求附送）」✓ ⑥失败任务卡片中文「失败」徽标+耗时 ✓ | 53,54 |
| B13 | seed 规则段 | 新建模板（模板 9）正文为**空**（源码 createTemplate promptBody:''）；现有 8 个内置模板正文尾部**均无**【贴钻指导规则】段（模板1全文仅 76 字符）——与 effectRefTemplatesV2.ts 注释「通用贴钻指导规则并入 v2 模板 seed 正文尾部」不符（可能是旧 IDB 数据未迁 v2 seed）——**FAIL/待复核** | — |

## C 排钻工作台（粗走查）

- 空态：未载入数字油画 + 「还没有数字油画」双入口（从素材库选择/直接上传）✓ · 55
- 载入 01-src.jpg：自动算钻 **3,886 钻/93.1 层/问题聚合✓**，色块列表 28 行（色号+数量）、右栏 排钻逻辑 hybrid/基底规格 round-ss10/gap 0.40mm 密度 100%/边界松弛/压力修复/色板 5 色/分块参数 k3、底栏 黑×2995 象牙白×459 橄榄绿×410 等 4 色、SVG/BOM CSV/PNG/送精修 ✓ · 56
- 色块行点选交互：右栏精调面板可达 ✓ · 57

## Console 错误

全程 `window.__errs`（error/unhandledrejection/console.error 钩子）**0 条**。

## 遗留状态（对主会话的交代）

- 假连接 gpt-dummy 仍配置在 localStorage；任务画廊存有 16 条失败/取消测试记录；素材库新增 01-src（上传入账）；保存了 R52走查项目.gemdoc；设计师文档图层 3/图层 1（测试层已并入图层 1）
- 「模板 9」（空模板）删除点击未被确认，可能残留于模板库
- ego TaskSpace 23 已 finish({keep:[]})，ego 应用与 5210 dev server 未动

---

## R5.2 走查回修批复注记（回修批 2026-09-21，主会话复核用）

- **B12 ③ 水钻画幅拦截「发起未拦」复核（P2-3 裁定：gate 读源正确，未修代码）**：startRun fail-fast 读的是模板 `drillParams.physical`（提交面）——与模板高级选项「画幅物理尺寸」同一真源，无「全局图宽/高 vs 模板画幅」读错源问题（全局图宽/高属高级参数请求尺寸，另一字段）。走查时未拦的两点解释：①「模板钻块画幅自动填 210×148」经代码核对为输入框 **placeholder（210/148）的判读**，物理声明实为空；②水钻开关拨开时若钻清单为空，走的是 `drillPendingOpen` **视觉待选态**（表单展开+必填红字渲染）而 store `enabled` 未落（validate 门拒写空清单）——gate 读提交面自然放行，属正确语义。已补测试锁定两口径（templateAdvancedOptions.test「P2-3 复核」：真留空→拦截已有既有断言；视觉待选态→正确放行）。重走查建议：拨开水钻→**选入至少一个规格**（使 enabled 真提交）→留空画幅→发起→应见中文拦截。
- **B13 seed 规则段 FAIL 复核（P2-4 已修）**：根因＝v2 模板 seed 是 create-only——本机 v2 节点在「DRILL_RULES 并入 v2 正文」（2e00c90）之前已 seed，正文不回写（WYSIWYG 语义设计使然）。按 v1→v2 换代先例 bump **v3**（`ast-tpl-<baseId>-v3` 带规则尾重播种；未修改 v1/v2 存量软删进回收站，用户改过的零触碰）。重走查预期：重启后模板库 8 条 v3（正文尾含【贴钻指导规则】），旧代进回收站。
- **A7 图层2 消失疑点（P2-5 jsdom 复现尝试：不可复现，留 9.3）**：已按自动化时序连发重命名流（新建图层→双击重命名→逐字符输入→blur 外点→Enter/Esc/切层连发）做 jsdom 复现——层数不变、行内输入零残留、名字不被竞态破坏（layersPanel.test「重命名时序竞态排查」）。走查所见「消失+零尺寸输入框残留」疑为自动化时序下的渲染假象（或与其他操作合序）。**留 9.3 真浏览器复核**；复现步骤如有回放请补记录。
- **A1 空态入口计数（P2-7 口径修正）**：实为**四入口**（选图新建/空白新建/打开/上传图片新建），清单口径过时——已改口径为四入口（代码注释/测试描述同步），入口本身零改动。

---

## R5.2 复验-R2 回修批注记（2026-09-21 第二轮，三聚焦点）

复验输入：P1-1 FIXED、P1-2 PARTIAL、⌘Z 全局失效（新发现）、纹理零加载（未修）。本轮全部经真浏览器实证（ego TaskSpace 25，5210 只读浏览）+ jsdom 回归锁定。

### A ⌘Z 键盘全局失效（已修）

- **根因（preventDefault 栈回溯实证）**：bits-ui Tabs 四个 Tabs.Content **常驻挂载**——StudioView 的 `<svelte:window onkeydown>`（studio ⌘Z → undoStudioOp）在设计师 Tab 激活期同样在听，且注册先于 DesignerView（App Tabs DOM 序）。⌘Z 被它先行 `preventDefault()` + 派发 studio undo（设计师上下文 no-op）；DesignerView 分派链三函数（handleToolKeydown/handleCommandKeydown/handleWorkbenchKeydown）均以 `event.defaultPrevented` 早退 → ⌘Z/⌘⇧Z 全局死。工具单键 B 等不滤 ⌘ 组合故幸存——与复验「按钮正常、键盘死」完全吻合。**变换态卡死/焦点滞留/popover 监听三个头号嫌疑全部排除**（实证：focus=DIV、B 键活、transform 盒已退）。
- **修复**：四个视图级全局键盘处理器统一加「活动视图守卫」（getView 门）：StudioView ⌘Z、StudioContextBar ⌘S（同族隐患——studio 载图后设计师 Tab ⌘S 会被吃）、DesignerView 全键分派（反向泄漏——studio Tab 裸键会切设计师工具/改笔刷径）、AssetsView document 级 Enter/Esc（Enter 会跨视图触发「打开选中项目」）。
- **验证**：真浏览器键位级——空白文档落钻 → ⌘Z 1→0、⌘⇧Z 0→1；⌘T 拖角柄 → Enter（2.8→15.95mm）→ ⌘Z 回 2.8（复验点名场景）。jsdom crossViewKeyboard.test 6 例（真实挂载序复现 StudioView 先注册；摘除守卫 4 红/装回 6 绿）。
- **为何上轮没抓住**：R5.2 走查从未测过 ⌘Z 键盘（只测 ⌘S/⌘T/⌥[——均不匹配 StudioView 的 ⌘Z 滤波，故全通过）；jsdom 组件测试单视图挂载，跨视图全局监听冲突不可见。

### B 精灵缓存失效/重载（已修——根因与上轮判定不同）

- **真根因（IDB 实况 + live 模块直驱实证）**：sys-shapes seed 只落 **20 个固定档**（round-ssXX + 四异形常用 mm 档），而钻直径连续——`marquise-13.43`（⌘T Enter 后）、属性面板改尺寸产出的任意精确档**永远无节点** → definitive miss → 永久几何回退。「不即时刷新/再做一次尺寸编辑立刻正确」＝尺寸编辑落在（或接近）seed 档位时纹理可解析的对照效应。**miss→烘焙→就绪通知→重绘链本身健康**（live 实证帧可达 + onGemSpritesChanged 触发）——上轮「通知丢失/脏帧」假说不成立。
- **「圆角方块+中心黑点」双拆解**：①「圆角方块」＝回退剪影把单位框映射到**直径×直径正方盒**（马眼 2:1 被画成方盒宽透镜）——gemVisual 剪影表缺纵横比；②「中心黑点」＝**笔刷光标本体**（draw/erase 工具下空心圆盘+中心点设计元素，rgba(15,23,42,.75)）叠在钻上——非渲染缺陷。回退剪影几何已按 engine SEED_ASPECTS 同源纵横比落内容盒（马眼真 2:1 透镜）。
- **修复**：gemSprites 纹理解析加**形级规范回退**——精确档 miss 且 builtin 形 → 取同形任一 seed 档纹理（艺术品与档位无关：seedOf 同形各档共用同一贴图；烘焙直径由请求侧提供）+ 形纵横比，shapeId 级记忆化；custom 仍精确缺席即 definitive。真浏览器实证：`marquise-2.8`（无精确节点）→ console 贴图就绪 → 815% 缩放像素测量 33×50 透镜（宽度剖面顶尖窄中段宽）。
- **为何上轮没抓住**：上轮修的是「首渲染早于 seed hydrate」竞态（TTL 可重试——对已 seed 档有效），未审「连续直径 vs 20 离散档」的覆盖缺口；jsdom 测试全部用注入 resolver，真实解析面（specKey→节点）无断言。

### C 纹理通路零发起（通路实证健康 + 测量学纠偏 + 诊断面）

- **IDB 实况（5210 同源直查）**：20 个 ast-shape-* 节点全在、blob 全可读（1.1KB 量级 .gemshape JSON 含 texture dataUrl）；runAssetMigration live 1ms completed（无 hang）；getProject/getImageBlob/parseGemshape 全链通。
- **管线 live 直驱**：真实页面 import gemSprites 模块 requestGemSprite（round-ss10/marquise-5/square-4）→ 帧烘焙成功（含柔投影 semi-alpha 像素 + multiply 着色）。**「整页 lifecycle resource 零图片请求」是测量学假象**：纹理加载走 `img.src = data: URL`——data: URL 不经网络栈，DevTools resource/network 面板**天然不收录**（blob: 同理多不可见）。「纹理零发起」结论不成立。
- **遗留真缺口＝B 的档位覆盖**（已随 B 修复：任意尺寸形级回退）。
- **诊断面（本轮新增）**：`[gemSprites] 贴图 miss（transient|definitive，Ns 后自动重试）：<specKey>` console.warn + `[gemSprites] 贴图就绪（烘焙入帧）：<specKey>` console.info（各 specKey 至多一次）。**复验正通道改为 console**——resource 面板对 data: 纹理永远显示为零，勿再以其为判据。
- **seed 艺术质量备注（未修，另立工作项）**：内置五形贴图为「银白渐变剪影」（内部亮度 std≈6.5——平坦渐变），R2 预期的「切面/高光纹理」属**贴图美术内容升级**（engine SEED_TEXTURES 数据换代 + seed 版本 bump），非通路缺陷，不在本轮回修面。
- **为何上轮没抓住**：上轮以「扇贝边平坦金片」判为回退常驻并修 TTL 重试；未做 resource 面板对 data: URL 的收录核实，也未直查 IDB 实况——两步本轮补齐后通路即证清白。

### 本轮测试清单

- 新增 `tests/designer/crossViewKeyboard.test.ts`（6 例：⌘Z 撤销/重做/Enter 确认后撤销/studio 反向隔离/B 键隔离/视图读取面）。
- 新增 `tests/designer/gemSprites.canonical.test.ts`（5 例：形级回退烘焙出帧/精确档优先/记忆化/custom definitive+warn/全档缺席+warn）。
- 扩展 `gemVisual.test.ts`（+4：纵横比表/内容盒/各向异性描形/h 缺省兼容）、`transformRenderChain.test.ts`（+1：马眼剪影 2:1 内容盒、正方盒形态不得出现）。
- 既有套回归：designer 全套 32 文件 388 绿；10 个挂载 DesignerView 的测试文件补 `setView('edit')`（守卫后挂载语义对齐 App 内编辑 Tab 激活等价）。
