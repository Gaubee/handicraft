# R5.2 全量重走报告（walkthrough-r2 · 2026-09-21 · TaskSpace 28 · 57 截图 /tmp/vision-walk4/）

裁决：**FAIL（P1×2 + P2×3）**——前两轮回修点全数通过；console 全程零 warn/error。

## P1
1. **PNG 导出未接线**：文档菜单→导出 PNG →toast「导出失败：PNG 渲染器未接线（四层合成栅格化后统一切片）。」（图 25，文案泄露内部注释）；SVG/BOM/gemdoc 正常。源码面 gemSprites 渲染链在位、导出管线未接（4.x 期已知偏离遗留）。
2. **水钻开+画幅真留空发起未拦截**：模板 9 开水钻、画幅宽高空（占位 210/148）→「开始生成×20」直接发起（图 45/46），无中文错误——与单测「真留空→拦截」矛盾，疑批量派发路径绕过 gate 或 enable 时自动填真值。

## P2
1. **图层重命名输入框不自动聚焦**（上轮「图层消失」定性：空名+输入框残留遮盖观感，层不丢；正常聚焦路径全好）。
2. **两个内建模板缺规则尾**：人物主体 v1（76 字）/英雄主体 v1（111 字）无【贴钻指导规则】段，其余 8/10 在位——疑走查轮次编辑痕迹致「改过不重播」或重播判定缺陷。
3. **⌥[ ⌥] 旋转幅度疑似小于标称 15°**（bbox 7→7.5px vs 预估 ~12px；keymap.ts:232-240 标称 ±15°/⇧5°）——量测方法（bbox）受起始角影响，需按 rotationDeg 字段实值核对。

## 通过面（摘要）
A2 马眼透镜 0.51 纵横比+纹理着色；A3 三态（hover +12.4 亮度/选中 #0284C7 蓝辉光）；A4 笔刷全链（默认单列/`]` 光标联动 31.5px≈8.8mm/大直径多列带/流量/popover 外点关+开态 ⌘Z 整带单撤销/橡皮红圈批量擦）；A5 选择系含 Alt 框减；A6 ⌘T 全绿（角柄 16→32px 与 205% 读数一致、钻位 407.3,426.8 分毫不动、Esc 零 patch 逐位全等、外柄旋转 +30.7° 实时）；A7 四 Tab 往返 ⌘Z 精确（397.3→390→397.3）；A8 批量改规格单撤销+点形即应用；A9 智能排布退役全页无痕；A10 隐藏层导出确认门取消零下载；A11 ⌘S→重开 41 钻完整+速查新键位全；B12 WYSIWYG 逐字零私货；B13 占位符注入/移除+留空=恢复自动语义；C 排钻粗查齐（21 色/hybrid/gap0.40/k8/出口四件）。

## 观察项（不计缺陷）
马眼柔投影弱于圆钻（alpha 峰 22 vs 242）；流量 30% 实测保留 ~59% 偏高（采样节奏未排除）；纹理=seed 银白渐变剪影设计性平滑（美术换代已另登记）；B15「高级参数三 Tab+尺寸 chips」本轮未定位到 UI（覆盖缺口，下轮聚焦核对——advancedParamsEditor 组件与测试在位）。

## 走查代理修复建议
①PNG 接线（短期可灰化菜单+去内部注释文案）→②空画幅全派发路径前置校验→③重命名 autofocus→④两模板规则尾→⑤⌥ 旋转实值核对；修完 P1 两项聚焦回归即可（其余已全量过）。
