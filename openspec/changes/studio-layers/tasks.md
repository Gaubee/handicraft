<!--
Orthogonal intents (max 5):
1. [2026-09-20 Contract] [R2 终审 W0-GATE-ONLY 后续授权范围 = 五段门序③④段，与 design §0.1 一致]：
     1.x（③ replay/handoff gate）：1.1（computeLayer 单一入口 + 兼容性 harness——一切层计算消费的共同前置，
          oracle fixture 必须在旧 runLayouts（studio.svelte.ts:649-729）退役前采集）
       -> 1.2 / 1.3 / 1.4（replay 层内核 / 联合 pairwise+exportGate 接线 / handoff v2+物理锚贯通——三者可并行，
          均只消费 W0+engine gate 冻结契约，文件域互斥）
       -> 1.5（gemprojReplay v2 六步链 + v1 fixture 逐位相等 + v1 读面删除——依赖 1.1-1.4 全部；
          projectFile.ts 写入与 gem-catalog 2.3 全局串行，design §0.4）-> 1.6（③段验收 receipt）
       -> 2.x（④ studio gate）：2.1 layers reducer / 2.2 history fold（可与 1.2-1.4 并行——纯 store 新建文件域）
            2.3 computeQueue（前置 1.1+2.1）；2.4 多选混合 / 2.5 观察态渲染三分（前置 2.1）
            2.6 PreviewRenderInput 层化（前置 2.5；旧 golden 迁移先行，缺迁移不切实现）
            2.7 UI 四区+面板族+废除清单（前置 2.1-2.6）；2.8 项目生命周期+dirty 全集（前置 1.5+2.1+2.2）
            -> 2.9（④段验收 receipt：消费面矩阵核销 + 死 API grep 清零）
       -> 3.x（收尾 gate：TERMS/PRODUCT_MODEL 升版 + 跨 change 依赖边就绪通知 + 全量绿门 + 评审归档候选）
   约束：并行实现代理上限 2；全量 pnpm test/check/build 绿门串行执行；computeLayer 兼容性证明前不切任何
   计算实现切片（R2 §四 P0-3）。
2. [2026-09-20 Data] LayerRecord（projectFile.ts:232）/v1→v2 迁移入口（:1286-1310）/PhysicalCanvas（engine/spec.ts:71）
   /pixelsPerMmFromCanvas（:234）/requiredCenterDistancePx·maxCellPx（engine/geometry.ts:52/:66）/exportGate
   （engine/exportGate.ts:93）= add-gem-catalog W0（d37b2a4→7790b69）+ engine gate（7f162d3→23bd262）已落库——
   本 change 只消费不重定义；v1 兼容读面 deriveLegacyGemprojView（projectFile.ts:916）删除归 1.5。
3. [2026-09-20 Store] studio 图层/历史/计算队列三子模块（.svelte.ts $state 宿主纪律 + 单向依赖 + 根聚合 re-export，
   沿 A 轨 2.1-2.6 先例）+ StudioOp 单真源（dirty 全集 = 一切 StudioOp——add-project-files 2.6 依赖边）；
   观察态族纯会话态不入档不入历史（工作默认一，待 Owner 批准可推翻，design §0.5）。
4. [2026-09-20 UX] 多选锚点=最早选中（selectionOrder[0]）；混合配置预填锚点值+「配置不同，以①层为基准」；
   触碰任一控件写入全部选中层=单 layer.config op 可整体撤销（工作默认二）；渲染三分固定常量（选中 1.0/
   非选中 0.8 不可调/背景 0.5 可调）；隐藏=观察态仍计算/统计/导出；GPU 不进 P0（CPU 恒 oracle——engine gate 已定）。
5. [2026-09-20 Process] 本 change 不做：①②⑤段、gem-catalog 2.x 资产化、add-project-files 2.6/2.7 实现、
   expert 5.7/5.9 接线（依赖边就绪通知归 3.2，design §0.3）；每步绿门 pnpm test + svelte-check + build；
   现状断言带 文件:行号；不宣称未实现内容为已完成。
-->

## 1. replay/handoff gate（③段）

- [x] 1.1 `computeLayer` 单一入口 + 兼容性 harness【一切层计算消费的共同前置】：新建 `src/lib/studio/computeLayer.ts`（lib 层编排函数——内部复用 `runCompute` 单请求形状 `strategies:[strategy]`，worker 协议零改动；契约语义 = 快照/run 号作废/取消/渐进落地，图层稿 §C.1）；**oracle fixture 先行采集**（现行五策略循环 `stores/studio.svelte.ts:649-729`（:667 子轮/:671-675 进度 1+i/6/:619-645 取消）退役前固化多图 × 五策略输出）；harness 六项矩阵（主断言：单 rest 层同参逐位相等 gems/warnings/dropped + 结果缓存/进度 1+N/取消 reject 身份/run 作废/错误隔离/重算期间旧结果保留）；退役面差异登记（五策略秒切 → 切策略即该层重算，Owner 授权）；vitest：harness 全矩阵
- [x] 1.2 replay 层模型内核（纯函数，jsdom 可测）：`resolveSpecForKey`（builtin 走 engine bootstrap `ROUND_SS_BOOTSTRAP`/`builtinSpecKey`，engine/spec.ts:91-103；custom 经注入目录 resolver；missing = typed 四态镜像 `ShapeAssetRefState`（exportGate.ts:47）禁静默降级圆钻）+ 层成员解析（唯一 rest 展开 = 分块结果 − 显式层并集；悬空键逐层清点——现状 v1 全局清点 gemprojReplay.ts:179-186 的层级化）+ 每层派生（effectiveBlocks/dispatch 两级密度回落（块覆写 ?? 层 density——getBlockDensity studio.svelte.ts:186-188 回落目标改写）/`gridFromSpec` 按层 specKey（engine/grid.ts:53；gridFromSs :67 降位特例不再消费））+ `pixelsPerMmFromCanvas`（engine/spec.ts:234）锚定实测宽 + 本地 `PIXELS_PER_MM` 副本（gemprojReplay.ts:41）切 engine 出口；vitest：rest 展开/显式并集互斥/悬空计数/两级回落/per-layer grid/非圆钻 custom 解析 typed 态
- [x] 1.3 联合 pairwise + exportGate 消费接线：全层 concat → `exportGate`（engine/exportGate.ts:93；cell=maxCellPx、判据 requiredCenterDistancePx×0.999 恒 px）；违规清单按层对分组（studio 侧按 gemId→层归属聚合为层内/层间——不改 engine 签名）；导出四路（SVG/BOM CSV/PNG/送精修）共同前置硬阻断、保存允许 warning；**八源碰撞清单逐条用例**（相邻块边界/各层独立 layout/不同径 gap/relax 位移/重分块归属/专家回流 gemdoc/malformed import 第二道防线/未来手工钻——R1 #9 全清单）；vitest：八源逐条 + 隐藏层仍参与 concat + 违规硬阻断 + 分组确定性
- [x] 1.4 handoff payload v2 + 物理锚贯通（**payload 修改权接管——A 轨薄 wrapper 换真，对照 expert design §2.2 交接表**）：`ManualEditHandoff`（stores/edit.svelte.ts:46 类型真源）扩 `physicalCanvas: PhysicalCanvas`；`buildManualEditHandoff`（studio/editHandoff.svelte.ts:43-62——现只复制单 activeResult+单 grid）换真：gems=各层 concat 逐钻物化规格（Gem 必含 shapeId/diameterMm——engine gate 1.4 makeGem 源头戳）、blocks=各层 effectiveBlocks 并集、sourceSummary 层语法 `N 层 · 共 X 钻 · 主规格 …`（现单值语法 :31-35 退役）；`loadFromHandoff` v2 消费（edit/gemdocLifecycle.svelte.ts:102——physicalCanvas 入 EditDocument（edit.svelte.ts:79 +字段）；v1 形态载荷 = default 锚向后兼容 quickLayout）；gemdoc round-trip physicalCanvas（serialize 位已冻结 projectFile.ts:336）字节等价；vitest：handoff identity（逐层 concat 断言）+ physicalCanvas round-trip（declared/default 两态）+ quickLayout 兼容
- [x] 1.5 gemprojReplay v2 六步链 + v1 读面删除（依赖 1.1-1.4）：`edit/gemprojReplay.ts`（:146-244）改写——segment 空轮（:170-174 保留）→ rest/显式层解析 → 每层派生（1.2 内核复用）→ 逐层 computeLayer（LAYOUT_SEED=1 同 seed 纪律 :45）→ 全层 concat 联合 pairwise/exportGate（1.3）→ handoff 逐钻快照+PhysicalCanvas（1.4）；v1 消费点（:166 gridFromSs(file.physics.ss)/:179-198 overrides/:202-213 单策略布局）全退役；逐层 onProgress 带层名 + AbortSignal 作废在途层轮；**v1 fixture 逐位相等**（迁移 projectFile.ts:1286-1310 后六步链输出 vs 旧 replay 路径：钻位/颜色/悬空清点逐项对照）；**v1 兼容读面删除**：`deriveLegacyGemprojView`（projectFile.ts:916-935）+ GemprojFile deprecated 三字段（physics/activeStrategy/overrides）一并移除【projectFile.ts 与 gem-catalog 2.3 全局串行——design §0.4】；vitest：六步链全链 + v1 fixture 相等三对照 + 非圆钻 specKey replay 直消费（读面 typed 拒绝解除）
- [x] 1.6 **③段验收 receipt**——R2 §四 P0-3 全文：「v1 fixture 迁移后与旧 replay 逐位相等；多层导出包含隐藏层且违规硬阻断；BOM 使用 `specKey×colorId`」（BOM 新键 engine 侧已证 2fddfcd，本段证多层接线路径）；R2 §四 P0-6 本方分项三元组（computeLayer/PhysicalCanvas——源码符号+测试文件+通过 receipt）；expert A 轨「无 payload 第二修改点」收据重核 + 5.7/5.9 解锁条件就绪登记（3.2 通知）

> **③段 replay/handoff gate receipt（2026-09-20 六切片全过；rhinestone-studio 逐切片 solo 绿门——ea68fc5(1.1)/77d3e0d(1.2)/4e1a88a(1.3)/a3e59ed(1.4)/cba2071(1.5)/本提交(1.6 收据)）**
>
> **R2 §四 P0-3 三分句验收**
> 1. 「v1 fixture 迁移后与旧 replay 逐位相等」——`tests/edit/gemprojReplay.test.ts`「v1 fixture 迁移后与旧 replay 逐位相等」2 例：v1 工程（顶层 physics/activeStrategy/overrides，含悬空键/颜色覆写/密度覆写/disabled 块）经 W0 迁移入口 parseGemproj 后六步链输出 vs 旧路径复刻（gridFromSs/rest 层物理/单策略布局/色覆写 oracle）：gems 全字段（id/x/y/colorId/blockId/shapeId/diameterMm）、droppedOverrides、blocks、grid、dimsMismatch 逐项相等；单 rest 层 ⇔ 旧整图单策略成立。
> 2. 「多层导出包含隐藏层且违规硬阻断」——`tests/studio/jointGate.test.ts`「隐藏层仍参与 concat（隐藏 ≠ 排除）」+「违规硬阻断信号」：jointExportGate 对 visible=false 层照常判据（组内层名含隐藏层），任一违规 → verdict.ok=false（导出四路共同前置的阻断信号；保存路径不消费本门）。全链形态：`tests/edit/gemprojReplay.test.ts` 六步链全链例——双半幅相邻层真实 inter 碰撞被第 5 步捕获（ok=false、仅 L1×L2 inter 组、无 intra 误报组）。
> 3. 「BOM 使用 specKey×colorId」——engine 侧键已证（2fddfcd buildBom/gemSpecIdentityOf）；本段多层接线路径：`tests/edit/gemprojReplay.test.ts`「逐层 concat」例——多层 handoff concat 逐钻 `gemSpecIdentityOf` 投影 specKey 按层分立（round-ss10/round-ss20 同集并存），非圆钻层逐钻规格快照按层物化（square 层 shapeId='square'/径 3.5——engine makeGem 恒 round 戳的层序物化补全）。
>
> **R2 §四 P0-6 本方分项三元组**
> - `computeLayer`：源码符号 `src/lib/studio/computeLayer.ts`（`computeLayer` 单一入口 + `LayerComputeSession` 逐层串行调度内核）；测试 `src/tests/studio/computeLayer.test.ts`（+ oracle fixture `src/tests/studio/fixtures/compute-layer-oracle.json`）；receipt：14 例绿——主断言「单 rest 层同参逐位相等」4 case × 五策略（gems 全字段/warnings/dropped 与旧子轮 oracle 逐位相等，密度 0.6/relax 开变体在内）+ 活路径锁定 4（现行 runLayouts 全管线输出 == oracle+mapColors，runLayouts 退役前恒成立，2.3 store 切换后复验）+ 六项矩阵（①结果缓存跨批保留/②进度 1+N 层名标签/③取消 ComputeAbortedError reject 身份/④run 作废迟到结果丢弃/⑤单层错误隔离/⑥重算期间旧 entry 可读）+ 进度层名标签。oracle 采集方法：驱动现行 studio store 全管线取真实派生参数（effectiveBlocks/densitySpec/grid/relax）→ 与 runLayouts 子轮（studio.svelte.ts:680-692）逐字段同构的 runCompute 单策略调用落盘 raw 输出（ORACLE_REGEN=1 再生纪律）。
> - `PhysicalCanvas`：源码符号 `src/lib/stores/edit.svelte.ts`（`ManualEditHandoff.physicalCanvas?` + `EditDocument.physicalCanvas` + `defaultPhysicalCanvasOf` 缺省锚合成单源）/`src/lib/edit/gemdocLifecycle.svelte.ts`（loadFromHandoff v2 消费/serializeCurrentGemdoc 恒写/loadFromGemdoc 旧档合成）/`src/lib/studio/editHandoff.svelte.ts`（buildManualEditHandoff default 锚）/`src/lib/edit/gemprojReplay.ts`（replay handoff file 直通·缺席 default 显式）；测试 `src/tests/edit/handoffV2.test.ts` 8 例绿（v2 declared 载荷/v1 形态 default 合成/quickLayout 真产物兼容/gemdoc round-trip declared+default 两态字段等价+serialize→parse→serialize 字节等价+loadFromGemdoc 装载面）+ `src/tests/edit/gemprojReplay.test.ts`（v1 工程缺席→default 锚、declared→直通+pixelsPerMm=canvas宽÷widthMm）+ W0 纯函数 `src/tests/engine/physicalAnchor.test.ts`（既有）。
>
> **expert A 轨「无 payload 第二修改点」收据重核（1.4 接管后）**：payload 符号族（ManualEditHandoff/buildManualEditHandoff/loadFromHandoff/EditDocument/physicalCanvas 贯通链）构造与结构修改点 = studio-layers 本段四处（stores/edit.svelte.ts · gemdocLifecycle.svelte.ts · editHandoff.svelte.ts · gemprojReplay.ts，git diff 1.4/1.5 提交实证）；其余命中（EditView/StudioStatusBar/documentService/imageSource/quickLayout/handoffImage/gemshapeFile/studio 根 re-export）均为纯消费/转发，零结构写入。5.7（PhysicalCanvas 真值贯通 EditDocument——已就绪：EditDocument.physicalCanvas 恒有值可读）/5.9（payload 首个后续修改解锁——③段验收完成即本 receipt）解锁条件就绪，登记归 3.2 通知。
>
> **偏离与实现裁决登记**（③段内，均已在实现报告列明）：① jointGate 判距 gap 层序组织 = 逐对 `max(两钻所属层 gapMm)`（各层单独过门本层 grid + concat joint grid 只保留层间对）——单 joint max-gap 会对小 gap 层自身合法布局产生假阳性（实证 965 例），逐对语义为「分区结果几何合规」的忠实实现；② 多层 concat 跨层 g##### id 序列全局重编号（各层引擎产物共用自增序列，单层=恒等映射，v1 逐位相等不受影响）；③ replay 逐钻规格快照按层物化（engine makeGem 源头戳恒 round——非圆钻层 shapeId/assetId 于 replay 物化补全）；④ replay jointGate 摘要非阻断（replay=转化路径非导出路径；四路硬阻断消费归 ④段 2.7）；⑤ sourceSummary 层语法分歧（quickLayout v1 形态容忍——design §5 议题 4，两测试面登记）。

## 2. studio gate（④段；2.1/2.2 可与 1.2-1.4 并行——纯 store 新建文件域）

- [x] 2.1 图层域 store `src/lib/studio/layers.svelte.ts`：LayerState reducer（id 自增 L1…/name/blockIds: string[]|'rest'/strategy/physics{specKey,gapMm,density,relax}/overrides 四表/visible）+ 运行时不变量守卫（恰一 rest/启用块恰属一层/显式互斥/并集⊆块集）+ 生命周期 op（create/delete[兜底层不可删+确认 Dialog]/merge[并集入锚点层]/rename/moveBlocks）+ LayerState↔LayerRecord 投影（纯函数，W0 类型消费不重定义）；现状单层全局态（studio.svelte.ts:116-128 覆写四表+全局物理/:132 selectedBlockId 单选）迁移；vitest：不变量全矩阵 + 生命周期 + 投影 round-trip
- [x] 2.2 历史域 store `src/lib/studio/history.svelte.ts`：StudioOp 八类（layer.create/delete/merge/rename/moveBlocks/config[layerIds[],patch,prev[]——多选批量=单 op]/block.override/palette.edit/segment.opts）+ `fold(baseSnapshot, ops) → {state, diagnostics}`（diagnostics 稳定 code/path 排序；stale op 只读灰显不删除不重写）+ 100 组压实（记被压实 op 边界起止 index + 压实后 state hash；跨压实边界 undo/redo 等价）+ ⌘Z/⇧⌘Z 与面板按钮同源 reducer + 新操作清 redo + 历史栈永不序列化；现状先例（edit UNDO_GROUP_BUDGET=100/stroke 合组/撤销栈不序列化）纪律平移不共用实现；vitest：fold 属性测试（同 base+ops ⇒ 同 state+diagnostics，跨重分块 undo/redo 全覆盖 + 撤销后新操作清 redo）+ 压实边界等价 + 瞬态族不入栈断言
- [x] 2.3 计算队列域 store `src/lib/studio/computeQueue.svelte.ts`【前置 1.1+2.1】：脏层追踪（dirtyLayerIds；debounce/immediate 双轨 CommitOpts studio.svelte.ts:205-207 语义平移；300ms 窗口合并）+ 单 worker 逐层串行（渐进落地/逐层错误隔离——现状单策略隔离 :704-716 层级化）+ 进度聚合 `{done:1+完成层数, total:1+脏层数, label:'层「名」排布中…'}` + cancelCompute 作废全部在途层轮；**store 面兼容性复验**（1.1 harness 在 store 接线后复跑）；根收缩：studio.svelte.ts（821 行）五策略域（results :137/activeStrategy :133/runLayouts :649-729/setActiveStrategy :554）退役（oracle fixture 已由 1.1 固化）+ $state 宿主纪律 + 根聚合 re-export + pnpm check 零消费方破坏；vitest：harness 复验 + 逐层调度序列 + 取消/作废 + 未触碰层缓存保留
- [x] 2.4 多选与「配置不同」【前置 2.1；工作默认二】：selectionOrder 有序选择集（单击重置/Cmd 尾部追加/Shift 范围/Cmd+A 全选普通层）+ 锚点=selectionOrder[0] 徽标 ①②③ + 字段级混合检测器（strategy/物理四件/覆写逐字段）+ 横幅「N 层 · 配置相同 | N 层配置不同 · 以 ①层名 为基准」+ 字段预填锚点值（不显示混合值）+ 触碰任一控件写入全部选中普通层=单 layer.config op（一次撤销恢复全部原值）+ 背景层可选入但批量物理写入只读 + 画布点选块=隐式单选所属层（命中失败不改选择——列表兜底）；vitest：混合面冻结矩阵（不同策略/spec/gap/relax/overrides 组合+空层选中态）+ 批量单 op 撤销 + 选择序语义
- [x] 2.5 观察态 + 渲染三分 + 背景层收编 previewMode【前置 2.1；工作默认一】：观察态族（层可见性/背景源与透明度/层块选择/取景/hover）不入档不入历史；背景层特殊层（钉底/不可删不可重命名/不参与排布统计导出；源 'none'|'painting'|'reference' + 透明度默认 0.5 可调）；previewMode 三模式收编（'gems'⇔源无/'painting'⇔数字油画/'reference'⇔参考原图；默认源=数字油画 50%——对现状 previewMode='gems'（studio.svelte.ts:134）的 Owner 授权默认值变更显式登记）；渲染三分常量（SELECTED_LAYER_OPACITY=1.0/DESELECTED_LAYER_OPACITY=0.8 固定不可调/BACKGROUND_OPACITY_DEFAULT=0.5）+ 合成序（白底→背景→普通层列表序→选中块高亮）；「隐藏≠排除」名义化（隐藏层仍计算/统计/导出 + 状态条「含 k 隐藏层」+ 只导出可见层须另设显式命令）；setPreviewMode/setOverlayOpacity（:558-564）与 StudioContextBar 消费点（:19-21/:57/:101）退役；vitest：观察态不入档（save→load 字节面）+ 隐藏层口径 + 三分常量渲染 + 收编映射
- [x] 2.6 PreviewRenderInput 层化 + golden 迁移【前置 2.5；**缺旧 golden 迁移不得切实现**】：新签名冻结（background{source,opacity,painting?,referenceBitmap?} + layers[{id,visible,selected,result}] + palette/blocks/size/dpr——mode/overlayOpacity/grid/result 单值入口废除）；旧三模式 golden 等价映射（每张补一张等价映射新签名卡逐字节相等：gems⇔source='none'/painting⇔source='painting'+旧 overlayOpacity/单 rest 层⇔layers=[单层 selected]；现 golden src/tests/studio/previewRender.test.ts）；`pickCanvasLayers`（previewRender.ts:129）与 BlockCanvas 分派（:214 消费）同步层化；逐钻形状渲染迁移（gemPaint.ts:32 gemRadiusPx(grid) 过渡重载 → gemRadiusPx(gem,grid) 逐钻签名——engine gate 1.3 偏离登记既定收口，本方文件域）；vitest：golden 等价迁移逐字节 + 新签名基准重立 + 层可见性/选中 alpha/背景源合成
- [x] 2.7 UI 拓扑 + 面板族 + 废除清单【前置 2.1-2.6】：StudioView 四区+左列（覆盖五区拓扑——src/lib/components/views/StudioView.svelte:3 头注/:106 胶片带挂载；「画布常驻/答案常驻/主区零滚动/min-h-0 链」不变量沿用）+ 左列 260px 双 tab（图层|历史）+ ⤺⤻ + LayerPanel/HistoryPanel 新建（行结构[眼睛][层名双击重命名][钻数][状态点 ◷/～/！/空]+选择序徽标+行尾菜单+[+新建图层]+背景层钉底行+键盘 ↑↓/Space；历史倒序列表+组合并+空历史态）+ Inspector 改版（层配置卡置顶：横幅区+策略 Select 全应用唯一写入点+物理组[规格 Select 数据源=gemCatalogService 接口——design §0.4 禁第二目录真源]+滑杆乐观 UI+300ms trailing）+ 选中块详情「移入图层▸」+ 折叠组（块列表只列选中层块/色板/分块参数破坏性警示升级）+ 上下文条瘦身（预览三模式+透明度滑杆废除）+ 状态条收窄（共 N 钻含隐藏层/M 层·含 k 隐藏层/联合校验徽标+分层违规清单▾+修复写入涉事层/SVG·BOM·PNG·送精修 exportGate 前置/进度+取消）+ **StrategyFilmStrip 废除清单执行**（组件 StrategyFilmStrip.svelte 192 行删除/setActiveStrategy API/results 五策略缓存/hover drawPreview 浮卡资产迁 P2 或删除/**exportFileName 去 -activeStrategy 后缀**（studio/exportSink.svelte.ts:52-54 → `${baseName}.${ext}`）+同名覆盖/PNG 入库命名/下载行为测试冻结+交互测试更新）；移动端 bottom sheet 同构（上下文条折两行+图层/历史抽屉+长按多选+导出▾）；vitest：布局冒烟（三档 viewport+左列可见性断言）+ 面板交互（重命名/合并/删除确认/徽标序）+ 废除后死 API grep 清零 + 状态矩阵 12 态清单（图层稿 §B.9）
- [x] 2.8 项目生命周期 + dirty 全集（承接 add-project-files 2.1-2.5 移交）【前置 1.5+2.1+2.2】：`src/lib/studio/projectPersistence.svelte.ts`——saveGemproj v2（fold 终态→serializeGemproj→库内 ingest/换绑复用 add-project-files 1.2 写路径与 lease/CAS；导出磁盘=embedded 烘焙；serializer 首写即 v2）+ 打开链路（openProject lease+pin source/reference→parse v2（v1 经迁移入口）→六步重放（1.5 同源）→恢复默认观察态+单次提示「不恢复上次观察布局」+engineVersion 漂移横幅+逐层覆写清点）+ 上下文条项目身份（项目名+●未保存+保存▾[另存为/导出项目文件/关闭]+⌘S+首次保存弹命名）+ 导出双路径（导出不清 dirty）+ 空态（最近≤4）与来源缺失守卫（错误卡+重绑+禁用占位）+ `isStudioDirty()`（一切 StudioOp 置 dirty/undo 不清——沿 edit 先例 edit.svelte.ts:15/保存清/导出不清）；vitest：save→load round-trip（层/配置/色板/分块/physicalCanvas/观察态缺席）+ dirty 全集 + 守卫三分法依赖读取器 + 漂移横幅 + 来源缺失重绑
- [x] 2.9 **④段验收 receipt**：消费面迁移矩阵逐行核销（图层稿 §E.6 全表——PreviewRenderInput/胶片带族/blockIndexAt[P0 兜底+P1 门登记]/全层统计/buildActiveSvg·Bom 全层/buildManualEditHandoff v2/gemprojReplay v2/exportFileName/runLayouts→computeLayer/asset pin 消费/gemdoc 烘焙分界/PIXELS_PER_MM 副本切换：每行源码符号+测试文件+通过 receipt 三元组）+ 死 API grep 清零（StrategyFilmStrip/setActiveStrategy/getPreviewMode/getOverlayOpacity/previewMode/activeStrategy 导出面）+ 消费端到端 jsdom 走查（载入→默认全选自动排布→建层/移块→多选混合批量写→撤销跨重分块→隐藏层导出口径→送精修 v2 载入）+ 全量 pnpm test/check/build

## 3. 收尾 gate

- [x] 3.1 TERMS v2→v3 / PRODUCT_MODEL v4→v5 升版：TERMS 增词条**图层**（禁用词：分组、分区）/**背景层**（禁用词：底图层）/**历史**（禁用词：撤销历史、操作日志）+「排钻项目」补 layers 描述 + **「层参数 vs 画幅物理锚」分词注记**（两个「物理」不得混用）；PRODUCT_MODEL 对象树「排钻设计·项目」扩图层[]+新增图层行+**真源表「排钻策略选择」宿主迁移**（胶片带→图层配置字段——现表即旧宿主）+新增「图层结构与配置」行+观察态边界注记；版本行登记；vitest/CI：术语与真源表 grep 核对（旧宿主措辞清零）
  〔2026-09-20 完成：TERMS v3 = 新词条 图层/背景层/历史/观察态（纯会话态——不入 .gemproj 不入历史）+「排钻项目」定义补 layers[] 图层化参数全集 + 新增「分词注记」节（层参数（层配置物理：specKey/gapMm/density/relax）vs 画幅物理锚（PhysicalCanvas：宽高 mm + pixelsPerMm）——两「物理」互不指代，R2 非阻塞建议落档）+ 版本头注登记升版缘由。PRODUCT_MODEL v5 = 版本行登记；一句话补图层；对象树新增「排钻设计·图层域」行（layers（LayerState reducer）/history（StudioOp fold 不记结果，永不序列化）/computeQueue（脏层追踪+单 worker 逐层串行）/projectPersistence（保存/打开/lease/dirty）四 store 子模块）+「排钻设计·项目」行扩「来源+layers[] 图层化参数全集」；流程图 studio 真源措辞图层化 + 送精修注（全层 concat 逐钻规格+画幅物理锚 physicalCanvas）+ 导出注（全层并集含隐藏层，exportGate 前置）；真源表：策略选择宿主迁移（图层配置字段·每层独立——节内旧宿主「胶片带」措辞清零，迁移史记版本行）+ 新增「图层结构与配置」行（唯一真源=layers 域 reducer；快捷入口=左列图层面板+检查器层配置卡）+ 新增「观察态」行（唯一真源=studio 会话内存，不入 .gemproj 不入历史）+「块覆写/物理参数」行改逐层口径；硬规则 2 措辞联动（策略唯一写入点=层配置卡）+ 新增 9（隐藏层仍参与计算/统计/导出——隐藏≠排除）与 10（多选批量=单 layer.config op，一次撤销恢复全部原值）。vitest/CI grep 核对落地 = 新增 `src/tests/docs/modelDocs.test.ts` 8 例（两文档版本登记/四词条与禁用词/排钻项目 layers/分词注记在位/对象树四子模块/真源表旧宿主清零+两新行/硬规则 9·10），随 pnpm test 入 CI；solo 8/8 绿 + svelte-check 0 错误。偏离注记：①「历史」禁用词限定命名面（面板/入口/标题）——守卫 Dialog 既有描述性短句「（含撤销历史）」（EditView 先例 + add-project-files 2.6 studio 同文）不属命名，保持不改；②源码注释中「胶片带」仅以废除史述及（LayerConfigCard/Inspector/StudioStatusBar 头注「胶片带废除后」），非现行宿主宣称，grep 不清零〕
- [ ] 3.2 跨 change 依赖边就绪通知（不改他方文件）：add-project-files 2.6（StudioOp dirty 全集+isStudioDirty 就绪——可收口）/2.7（gemproj 打开链路端到端消费者就绪）；rename-and-expert-workbench 5.7（PhysicalCanvas 真值贯通 EditDocument）/5.9（③段验收完成——payload 首个后续修改解锁）；gem-catalog 2.4（三处 PIXELS_PER_MM 副本断言随本方切换闭合状态核对）；归档时⑤段依赖边（add-project-files design §10）状态同步
- [ ] 3.3 全量绿门 + Codex 评审 → 修订 → 归档候选；**最终合流门逐字引用收口**：R2 §四 P0-3/P0-6 验收原文与本 change receipt 对照 + R2 §六「③④段完成前不得宣称两稿闭合」的解除声明 + 消费面矩阵（图层稿 §E.6）全行核销证据落档
